# Template only. Never apply this file or commit real credentials.
# Create the named secrets through the approved GoI secret manager / CI pipeline.
apiVersion: v1
kind: Secret
metadata:
  name: database-secret
  namespace: ipsakti
type: Opaque
stringData:
  username: "REPLACE_AT_DEPLOY_TIME"
  password: "REPLACE_AT_DEPLOY_TIME"
  url: "postgresql://REPLACE_AT_DEPLOY_TIME"
---
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secret
  namespace: ipsakti
type: Opaque
stringData:
  admin-username: "REPLACE_AT_DEPLOY_TIME"
  admin-password: "REPLACE_AT_DEPLOY_TIME"
  client-secret: "REPLACE_AT_DEPLOY_TIME"
---
apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: ipsakti
type: Opaque
stringData:
  access-key: "REPLACE_AT_DEPLOY_TIME"
  secret-key: "REPLACE_AT_DEPLOY_TIME"
